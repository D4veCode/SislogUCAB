# SislogUCAB V.0.0.1

## Sistema para el manejo de entregas de paquetes

Existen dos partes en este proyecto, el frontend, hecho en React y el API hecho en Flask. 

Para iniciar el backend, crear un virtual environment y luego ejecutar `pip install -r requirements.txt` para luego escribir el comando `python3 run.py`.

Para iniciar el frontend, ejecutar el comando `npm install`
